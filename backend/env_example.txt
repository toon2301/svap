# Django settings
DEBUG=True
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database
DATABASE_URL=sqlite:///db.sqlite3

# Redis (cache + Celery broker)
REDIS_URL=redis://localhost:6379/0
# Optional: auth metadata cache TTL in seconds (default in code is 86400 / 24h)
# AUTH_USER_REDIS_CACHE_TTL_SECONDS=86400
# Optional: required when using PgBouncer transaction pooling with Django
# DB_DISABLE_SERVER_SIDE_CURSORS=True
# Optional: keep DB connections warm in WSGI HTTP service (default in code is 300)
# DB_CONN_MAX_AGE=300
# Optional: fail fast on slow Redis instead of retrying timed-out cache ops
# CACHE_RETRY_ON_TIMEOUT=False
# Optional: on a degraded cold miss, skip the second slow auth cache write
# AUTH_USER_CACHE_SLOW_GET_SKIP_SET_MS=250

# CAPTCHA settings
CAPTCHA_ENABLED=True
CAPTCHA_SECRET_KEY=your-google-recaptcha-secret-key
CAPTCHA_SITE_KEY=your-google-recaptcha-site-key
CAPTCHA_SKIP_IN_TESTS=True

# Email settings
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=your-email@gmail.com

# CORS settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Rate limiting
RATE_LIMITING_ENABLED=True
RATE_LIMIT_DISABLED=False

# Audit logging
AUDIT_LOGGING_ENABLED=True

# SafeSearch (Google Cloud Vision)
SAFESEARCH_ENABLED=True
SAFESEARCH_FAIL_OPEN=True
SAFESEARCH_TIMEOUT=5
SAFESEARCH_TIMING_LOG=False
SAFESEARCH_MIN_ADULT=POSSIBLE
SAFESEARCH_MIN_VIOLENCE=LIKELY
SAFESEARCH_MIN_RACY=LIKELY

# Offer images pipeline (async)
# - upload goes to S3 prefix: uploads/
# - worker writes approved outputs to: media/
OFFER_IMAGE_MAX_SIDE=1600
OFFER_IMAGE_WEBP_QUALITY=82

# Local credentials path (optional if using JSON in env)
# GOOGLE_APPLICATION_CREDENTIALS=backend/.secrets/gcp-vision.json

# ---------- Production / Railway (oddelený frontend a backend) ----------
# DEBUG=False
# Pre cross-site cookies (FE a BE na rôznych Railway URL) nastav:
# RAILWAY=True
# FRONTEND_ORIGIN=https://tvoja-frontend-app.up.railway.app
# CORS_ALLOWED_ORIGINS=https://tvoja-frontend-app.up.railway.app
# CSRF_TRUSTED_ORIGINS=https://tvoja-frontend-app.up.railway.app,https://tvoj-backend.up.railway.app
# FRONTEND_CALLBACK_URL=https://tvoja-frontend-app.up.railway.app/auth/callback
# BACKEND_CALLBACK_URL=https://tvoj-backend.up.railway.app/api/oauth/google/callback/
# Pri cross-origin OAuth backend automaticky pridá tokeny do redirect URL, aby prihlásenie fungovalo.
#
# Split runtime pre vyšší výkon na Railway:
# 1. HTTP service:
#    APP_SERVER_MODE=http
#    RUN_STARTUP_TASKS=1
#    Start Command: ./start.sh
# 2. WebSocket service:
#    APP_SERVER_MODE=ws
#    RUN_STARTUP_TASKS=0
#    Start Command: ./start.sh
#
# Frontend proxy:
#    BACKEND_ORIGIN=https://tvoj-http-backend.up.railway.app
#    BACKEND_WS_ORIGIN=https://tvoj-ws-backend.up.railway.app
#    NEXT_PUBLIC_BACKEND_ORIGIN=https://tvoj-http-backend.up.railway.app
#    NEXT_PUBLIC_BACKEND_WS_ORIGIN=https://tvoj-ws-backend.up.railway.app
#
# Kompatibilný fallback (jeden backend service):
#    APP_SERVER_MODE=all
#    Start Command: ./start.sh
